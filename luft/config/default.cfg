# ──────────────────────────────────────────────
# luft default configuration
# Lines starting with # are comments
# Format: key = value
# ──────────────────────────────────────────────

# Simulation
time_step = 0.01
max_sim_time = 0

# Network
networking_enabled = true
telemetry_host = 0.0.0.0
telemetry_port = 5000
command_host = 0.0.0.0
command_port = 5001
telemetry_rate_hz = 20.0

# Logging
log_level = info
log_file =
log_console = true

# UI
ui_enabled = true
window_width = 1280
window_height = 720

# Aircraft
aircraft_type = cessna172
initial_altitude_m = 1000.0
initial_airspeed_ms = 50.0
initial_heading_deg = 0.0
initial_fuel_kg = 100.0

# Environment
wind_north_ms = 0.0
wind_east_ms = 0.0
wind_down_ms = 0.0
gust_intensity = 0.0

# Control sensitivity
elevator_sensitivity = 1.0
aileron_sensitivity = 1.0
rudder_sensitivity = 1.0
