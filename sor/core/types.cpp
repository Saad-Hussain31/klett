#include "core/types.h"

// All core type implementations are inline / header-only.
// This translation unit exists to ensure the header compiles cleanly
// and to provide a link target for the static library.
